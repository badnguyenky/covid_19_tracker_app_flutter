class ExpansionPanelModel {
  bool isExpanded;
  final String header;
  final String body;
  final String image;
  ExpansionPanelModel(
      {this.isExpanded: false, this.header, this.body, this.image});
}
